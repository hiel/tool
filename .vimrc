call plug#begin()

Plug 'doums/darcula'

call plug#end()

colorscheme darcula
set termguicolors "true color 지원
set hlsearch "검색어 강조
set nu "줄번호
set autoindent "자동 들여쓰기
set shiftwidth=4 "자동 들여쓰기 너비
set ts=4 "탭 크기
set autoread "파일이 외부에서 변경되면 자동 불러오기
set laststatus=2 "상태바 항상 표시
set showmatch "일치하는 괄호 강조
set smartcase "검색시 대소문자 구분
set ruler "현재 커서 위치 표시
